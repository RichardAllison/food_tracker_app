<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.richardallison.foodtracker">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ViewFoodDatabaseActivity"
            android:label="@string/food_database"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".CreateFoodActivity"
            android:label="@string/create_food"
            android:parentActivityName=".ViewFoodDatabaseActivity" />
        <activity
            android:name=".ViewAllRecordsActivity"
            android:label="@string/user_records"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".CreateRecordActivity"
            android:label="@string/new_record"
            android:parentActivityName=".ViewAllRecordsActivity" />
    </application>

</manifest>